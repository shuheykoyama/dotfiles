# ~/zsh/zsh_plugins/history.zsh

# Share command entry histories between different tabs and windows.
setopt share_history

# Number of histories saved in the memory.
export HISTSIZE=1000

# Number of histories saved in history file.
export SAVEHIST=10000
